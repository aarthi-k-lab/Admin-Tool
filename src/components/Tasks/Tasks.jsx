import React from 'react';

import './Tasks.css';
import CollapseIcon from './CollapseIcon';

const Tasks = () => (
  <section styleName="container-name">
    <header styleName="header">
      <span>Tasks</span>
      <CollapseIcon />
    </header>
    <article>
      test
    </article>
  </section>
);

export default Tasks;
